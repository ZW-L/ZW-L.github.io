## [net](http://nodejs.cn/api/net.html)

+ net.Server
+ net.Socket
+ net.connect()
+ net.createConnection()
+ `net.createServer([options][, connectionListener])`：
+ `net.isIP(input)`：
+ `net.isIPv4(input)`：
+ `net.isIPv6(input)`：
